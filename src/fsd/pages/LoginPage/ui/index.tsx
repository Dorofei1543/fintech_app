import { LoginForm } from "@/fsd/widgets/LoginForm";

const LoginPage = () => {
  return (
    <div className="flex flex-col items-center justify-center h-[95vh]">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
